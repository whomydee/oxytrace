
# all the environment related skeleton variables for the project

# Google Drive URL for the dataset CSV file
# Use the sharing link from Google Drive (e.g., https://drive.google.com/file/d/FILE_ID/view?usp=sharing)
DATASET_URL=https://example.com/your-dataset-link

# Logging configuration
# LOG_LEVEL options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Runtime environment: "local" for console logs, anything else for JSON logs
RUNTIME_ENVIRONMENT=local